##################################
# Fichier de configuration d' i3 #
##################################

set $mod Mod4
set $alt Mod1

# Déplacement d'une fenêtre en mdoe floating
floating_modifier $mod

# Verrouille l'écran
bindsym $mod+l exec i3lock -i ~/.config/i3/img/lockscreen.png -t

# Lance un terminal
bindsym $mod+Return exec i3-sensible-terminal

# Screenshot
bindsym Print exec scrot -u '%d-%m-%Y_%H-%M-%S.png' -e 'mkdir -p ~/Images/Screenshots && mv $f ~/Images/Screenshots && xclip -selection clipboard -t image/png -i ~/Images/Screenshots/`ls -1 -t ~/Images/Screenshots | head -1`' # All screens

# Ferme le fenêtre
bindsym $mod+Q kill

# Lancement des menus
bindsym $mod+a exec --no-startup-id "rofi -show drun -font \\"DejaVu 9\\" -run-shell-command '{terminal} -e \\" {cmd}; read -n 1 -s\\"'"
bindsym $mod+d exec --no-startup-id "rofi -show run -font \\"DejaVu 9\\" -run-shell-command '{terminal} -e \\" {cmd}; read -n 1 -s\\"'"

# Change le focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Déplacement de la fenêtre
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Mode horizontal
bindsym $mod+h split h

# Mode vertical
bindsym $mod+v split v

# Mode plein écran
bindsym $mod+f fullscreen toggle

# Change le layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+z layout tabbed
bindsym $mod+e layout toggle split

# Activation du  tiling / floating
bindsym $mod+Shift+space floating toggle

# Déplacement des workspace sur les autres écrans
bindsym $alt+Left move workspace to output left
bindsym $alt+Down move workspace to output down
bindsym $alt+Up move workspace to output up
bindsym $alt+Right move workspace to output right

# Définition des workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# Changement de workspace
bindsym $mod+ampersand workspace $ws1
bindsym $mod+eacute workspace $ws2
bindsym $mod+quotedbl workspace $ws3
bindsym $mod+apostrophe workspace $ws4
bindsym $mod+parenleft workspace $ws5
bindsym $mod+minus workspace $ws6
bindsym $mod+egrave workspace $ws7
bindsym $mod+underscore workspace $ws8
bindsym $mod+ccedilla workspace $ws9
bindsym $mod+agrave workspace $ws10

# Change le focus des fenêtre du workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+eacute move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+egrave move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+ccedilla move container to workspace $ws9
bindsym $mod+Shift+agrave move container to workspace $ws10

# Rechargement du fichier de configuration
bindsym $mod+Shift+c reload

# Redémmarrage de i3
bindsym $mod+Shift+r restart

# Quitter i3 (fermeture de X)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Vous avez utilisé le raccourci pour quitter i3, voulez vous vraiment quitter i3 ?.' -B 'Oui, quitter i3' 'i3-msg exit'"

# Mode resize
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym m resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# Activation de mode resize
bindsym $mod+r mode "resize"

# Gaps
gaps inner 10
gaps outer 6

# Suppression de la barre de titre
for_window [class=".*"] border pixel 0
client.focused #262626 #262626 #ffffff #262626 #262626

# Assignation des workspaces
assign [class="Brave"] $ws1
exec --no-startup-id brave

assign [class="code"] $ws3

assign [class="discord"] $ws9
exec --no-startup-id discord

# Scraptchpad terminal
bindsym $mod+t [instance="dropdown"] scratchpad show; [instance="dropdown"] move position center
for_window [instance="dropdown"] move scratchpad
for_window [instance="dropdown"] border pixel 5
for_window [instance="dropdown"] resize set 1225 800

# Scraptchpad shortkey
bindsym $mod+k [instance="shortkey"] scratchpad show; [instance="shortkey"] move position center
for_window [instance="shortkey"] move scratchpad
for_window [instance="shortkey"] border pixel 5
for_window [instance="shortkey"] resize set 887 900
	
# AUTORUN
exec --no-startup-id dunst 
exec --no-startup-id feh --bg-fill ~/.config/i3/img/background.png
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always --no-startup-id compton --config ~/.config/compton/compton.conf
exec_always --no-startup-id autorandr -c
